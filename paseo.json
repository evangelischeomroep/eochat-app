{
  "worktree": {
    "setup": [
      "if [ -x \"$HOME/.agents/projects/conduit/setup-worktree\" ]; then \"$HOME/.agents/projects/conduit/setup-worktree\"; fi",
      "git submodule update --init --recursive",
      "flutter pub get",
      "dart run build_runner build"
    ]
  }
}
